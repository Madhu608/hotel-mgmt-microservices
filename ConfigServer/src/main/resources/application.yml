server:
  port: 8888

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Madhu608/microservices-config-server
          default-label: main
          clone-on-start: true
          timeout: 4

